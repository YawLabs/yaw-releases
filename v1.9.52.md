# v1.9.52

- fix(wizard): show the Git Bash note only on Windows
- test(linux): cover self-heal SUID, idempotency, missing/CWD chrome-sandbox, postrm legacy targets
- fix(linux): stop postinst clobbering the maker's /usr/bin/yaw symlink
- Fix per-run Mac host release .cmd: anchor to AllocationTime + 24h05m, retry on HostMinAllocationPeriodUnexpired, self-cleanup
