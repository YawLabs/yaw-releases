# v1.3.10

- Extract session-merge policy into a pure, testable function (#23)
- types / defaults polish (#22)
- Teardown + error-path polish batch (#21)
- Overlay hygiene: pid-aware sweep + shared CLAUDE.md helper (#20)
- Unify platform detection into shared/platform-detect (#19)
- Harden ai-provider: stream timeouts, error-body cap, Gemini key in header (#18)
