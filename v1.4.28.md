# v1.4.28

- fix(broadcast): self-referential pane id hung CI tests for hours
- Broadcast perf: PTY_WRITE_MANY fanout + validation defence
- Broadcast mode hardening: per-pane participation + Esc + paste neutralization
- Round 5: extract CLI parser to its own module + harden test seams
- fix(test): Windows-flaky rename-after-sync race + ENOTEMPTY rmSync retry
