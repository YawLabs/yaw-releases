# v0.9.710

1. **Multi-window session restore** — stable windowKey UUIDs, merge logic, macOS dock-reopen preservation
2. **Terminal fixes** — bottom descender clipping, idle-scroll stalls, WebGL ref race, write-buffer drop logging
3. **Editor nav overlay** — top/bottom arrow buttons inside the bundled micro editor + termtitle fix so all editor features actually trigger
4. **Tailscale SSH username auto-detect** — key-based probe with candidate fallback, install-OpenSSH dialog, Retry flow, result cache
