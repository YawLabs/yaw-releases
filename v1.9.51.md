# v1.9.51

- collapsible settings sections (ai providers, keyboard shortcuts)
- fix(terminal): detect yaw typed/t launcher as AI CLI on non-typed providers
- fix(terminal): prevent SGR attribute leak when the write-buffer cap drops bytes (#91)
