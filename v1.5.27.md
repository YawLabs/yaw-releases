# v1.5.27

- pty-manager: post-merge fix for parallel-fan-out tests under POOL_SIZE=4
- yaw-mode: decouple model + effort from spawn-time pool snapshot
- pty-manager: bump PRE_SPAWN_POOL_SIZE 2 -> 4
- pty-manager: parallel pool refill after wide invalidation
- TerminalView: escape-aware + wrap-aware local-echo
- TerminalView: pre-attach local-echo + /yaw-review punch list
- TerminalView: review-loop punch list on the atlas-recovery hooks
- TerminalView: heavy reattach on user interaction when atlas corruption flagged
