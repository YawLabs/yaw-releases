# v1.6.12

- fix(yaw-mode): leave policy undefined for non-npm CC, don't lock out future migration
- fix(yaw-mode): skip consent prompt for non-npm CC installs
- perf(terminal): drop paneId/paneCount from zoom-toggle effect deps
- feat(yaw-mode): pin claude-code CLI version in manifest
- fix(terminal): heavy WebGL reattach on background-pane reveal after zoom
