# v1.3.17

- perf(auto-updater): preserve primed cache in the nuclear-reinstall fallback (#39)
- perf(auto-updater): launch splash first, narrow shortcut-fix scan (#38)
- perf(auto-updater): consolidate WMI kill-sweep and tighten PID-exit poll (#41)
- obs(auto-updater): add grep-able branch tags to quitAndInstall PS script (#42)
- perf(release): widen immutable cache-control to all platform artifacts (#40)
