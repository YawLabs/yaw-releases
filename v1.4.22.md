# v1.4.22

- fix(test): use path.join for Volta candidate path in resolver test
- fix(yaw-mode): resolve npm executable with PATH-stale fallbacks
- fix(yaw-mode): force claude postinstall + repair broken native binary
