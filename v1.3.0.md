# v1.3.0

<!--
Edit this draft into user-facing release notes.  Group by theme, trim
commits that aren't worth announcing, and frame things from the user's
perspective (not the commit-message perspective).  Save and close the
editor to continue publishing.
-->

- feat(yaw-mode): migrate rules to markdown bundle + CLAUDE.md @-imports (v1.3.0)
- chore: extract bench harness to YawLabs/yaw-benchmark
- docs(attribution-proposal): lock Phase 1 decisions
- docs: Phase 1 attribution upgrade proposal
- feat(yaw-mode): attribution honesty clauses in skills + agents
- feat(eval): Phase 1 entry — context-tax measurement (#8)
- chore(eval): Phase 0 close-out — parser extraction, compare tool, roadmap
- fix(eval): normalize CRLF in task files so frontmatter parses on Windows
- fix(eval): mirror .credentials.json sync-back into harness + overlay port
- fix(yaw-mode): copy overlay .credentials.json back to home on teardown
- feat(eval): Phase 0 harness — yaw-overlay variant + multi-turn task
