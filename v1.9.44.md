# v1.9.44

- test(e2e): add MCP panel render smoke (mcp-panel.spec.ts) (#89)
- feat(mcp-panel): inline catalog browse-and-add in McpManager (#87)
- fix(ci,linux): postinst uses readlink -e so missing bin link actually warns; de-flake FSEvents fingerprint test
