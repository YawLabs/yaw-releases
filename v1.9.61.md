# v1.9.61

- test(connections): cover the non-string cached-name collision-scan guard (#130)
- test(connections): cover handleImport error / all-rejected / partial-failure paths (#129)
- fix(connections): sanitize imported connections (allowlist, credential strip, port-range) (#127)
- fix(team): harden shared-resource store, sync, and IPC error surfacing (#125)
- fix(mcp): harden catalog refresh, audit race/timeout, and abort surfacing (#126)
