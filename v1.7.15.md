# v1.7.15

- install-software-probe: pin fetch signal + darwin PATH separator
- yaw-mode: platform-windows -- correct npm-arm64 segfault scope (not MSYS)
- install-software-probe: cover execFile opts + darwin probe env cache
- yaw-mode: platform-windows -- npm-run segfault on MINGW64-ARM
- install-software-probe: unit tests + azure-pacman branch coverage
- package-lock: add brace-expansion 2.1.0 / balanced-match 1.0.2 transitives
- team-sync + RemoteSessions: race-safety follow-ups + brace-expansion override
- team + license + remote-sessions: hardening
- install-software: wizard polish, path-fixture rule
