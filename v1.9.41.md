# v1.9.41

- fix(yaw-mode): follow-up review fixes for yaw-watch + memory-curator
- chore(yaw-mcp): scrub remaining dead-brand refs (fixture + archive gameplan)
- fix(yaw-mcp): converge MCP server key to `mcp` (namespace mcp__mcp__)
- docs: rename deprecated mcp-hosting -> yaw-mcp in proposal docs
- test(connections): cover cache contract, unknown-type throw, mongosh dispatch
- fix(connections): paint the unknown-type connect error red, not green (#86)
- test(remote-sessions): cover multi-session tmux parse + cross-line not-found guard
- checkpoint(connections): in-progress full-pass hardening -- team-resource-store, keybindings, sidecar, rules
- fix(remote-sessions): address adversarial-review findings (12)
- fix(tooling): dedupe local vs origin branch by name, fix sort-order winner
- fix(connections): guard buildConnectionCommand call sites in App + store
- fix(yaw-mode): skills + rules audit fixes, plus 2 new rules
- feat(mcp): wire Yaw MCP panel keybinding + harden sidecar/migration
- fix(connections): harden connection load/build paths from full-pass
- fix(tooling): exit-code-aware conflict check + --no-fetch + honest read-only claim
- fix(remote-sessions): precise capability check + surface connection failures
- perf: gap-slice optimizations (ui-dialogs + background-mgrs, 17 items)
- perf: hot-path optimizations from the verified full-pass (45 items)
- tooling: add read-only merge-readiness survey script
- fix(yaw-mode): escape every dot in downshift drop-rule regex
- test(e2e): trim yaw-mode ultracode spec to its GUI seam
- test(e2e): trim yaw-mode fresh/augment specs to their GUI seams
- @ test(e2e): trim provider-switch spec to its GUI seam
- docs: flag stale-env banner bug (source-once short-circuit)
